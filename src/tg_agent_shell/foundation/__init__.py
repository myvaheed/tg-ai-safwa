"""What every layer above may name: the clock, the errors, the base row, a screen, the
loop a background task runs in, and what kind a bot message is.

Each is free of any application's vocabulary, which is what lets the package travel.
"""

from __future__ import annotations
